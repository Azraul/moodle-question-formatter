[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "moodle-question-formatter"
version = "0.1.0"
description = "Convert plain text quiz questions to Moodle XML"
requires-python = ">=3.10"

[project.scripts]
build-quiz = "build_quiz.cli:main"
